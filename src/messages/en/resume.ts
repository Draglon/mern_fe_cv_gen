import { RESUME } from "@/messages/common/resume";

const resume = {
  "Template": {
    "layout": {
      "title": "Resume",
      "description": "Resume - description"
    },
    ...RESUME
  },
};

export default resume;
